defmodule MeshxMob do
  @moduledoc """
  Mobile-specific utilities, platform abstractions, and optimizations for MeshX.

  `MeshxMob.Platform` captures platform state that affects transport behavior,
  including background mode, permissions, native bridge module, and metadata
  advertised to peers.
  """
end
